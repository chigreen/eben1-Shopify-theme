<!-- Hero Section - EBEN1 Speed -->
<section class="hero-section">
  <div class="hero-content">
    <h1 class="hero-title">{{ section.settings.title | default: 'One Gear' }}</h1>
    <p class="hero-subtitle">{{ section.settings.subtitle | default: 'Engineered for Singular Purpose. Zero Compromise.' }}</p>
    <div class="hero-actions">
      <a href="{{ section.settings.cta_link | default: '/collections/all' }}" class="btn btn-primary">
        {{ section.settings.cta_text | default: 'View Builds' }}
      </a>
      <a href="{{ section.settings.secondary_link | default: '/pages/about' }}" class="btn btn-secondary">
        {{ section.settings.secondary_text | default: 'The Blueprint' }}
      </a>
    </div>
  </div>
  {% if section.settings.background_image %}
    <div class="hero-background">
      <img src="{{ section.settings.background_image | image_url: width: 1920 }}" alt="" loading="eager">
    </div>
  {% endif %}
</section>

{% schema %}
{
  "name": "Hero",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "One Gear"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Engineered for Singular Purpose. Zero Compromise."
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "Primary button text",
      "default": "View Builds"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "Primary button link"
    },
    {
      "type": "text",
      "id": "secondary_text",
      "label": "Secondary button text",
      "default": "The Blueprint"
    },
    {
      "type": "url",
      "id": "secondary_link",
      "label": "Secondary button link"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background image"
    }
  ],
  "presets": [
    {
      "name": "Hero"
    }
  ]
}
{% endschema %}
